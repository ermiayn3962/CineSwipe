<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="tempstyle.css">
        <title>Create Account</title>

        <script 
            src="https://code.jquery.com/jquery-3.7.1.js" 
            integrity="sha256-eKhayi8LEQwp4NKxN+CfCh+3qOVUtJn3QNZ0TciWLP4=" 
            crossorigin="anonymous">
        </script>
    </head>

    <body>
        <div class="wrapper">
            <form action="/signup" method="post">
                <h1>New Account</h1>
                <div class="input-box">
                    <label for="name">Username</label>
                    <input class="input" type="text" id="name" name="name" placeholder="user123" required>
                </div>
                <div class="input-box">
                    <label for="email">Email</label>
                    <input class="input" type="text" id="email" name="email" placeholder="name@example.com" required>
                </div>
                <div class="input-box">
                    <label for="password">Password</label>
                    <input class="input" type="password" id="password" name="password" placeholder="********" required>
                </div>
                
                <button class="btn" type="submit">Create Account</button>
            </form>
            <a href="/login" class="alr"><br>I have an account already</a>
        </div>
    </body>
    
    <!-- <script>
        $('document').ready(() => {
            console.log("hi")
            let form = $("form").on('submit', async (e) => {
                const form = document.querySelector('form')
                const fd = new FormData(form);
                const urlEncoded = new URLSearchParams(fd).toString();

                e.preventDefault();

                var data = await fetch('http://localhost:3000/api/signup', {
                    method: 'post',
                    body: urlEncoded,
                    headers: {
                        'content-type' : 'application/x-www-form-urlencoded'
                    }

                })
                .then(res => res.json())

                console.log(data.message)

                if (data.message === 'User created successfully') {
                    window.location.href = 'http://localhost:3000/tinder.html';
                } else {
                    /* Resetting Previous Search Results */
                    let element = document.getElementById('errorMessage')
                    while (element.hasChildNodes()) {
                        element.removeChild(element.firstChild)
                    }

                    let message = $("<p>").text(data.message);
                    $("#errorMessage").append(message)
                }
            })
        })
    </script> -->

</html>





</html>
